# CodeGuard — Python dependencies (AWS Lambda + Bedrock)
# ─────────────────────────────────────────────────────────────────────────────
# To build the deployment package for Lambda (Linux x86_64):
#
# pip install --platform manylinux2014_x86_64 --target package/ --only-binary=:all: --upgrade -r requirements.txt
# cp server.py,lambda_handler.py,dynamo_memory.py,secrets.py package/
# Compress-Archive -Path package/* -DestinationPath codeguard-lambda.zip -Force
# ─────────────────────────────────────────────────────────────────────────────

fastapi
uvicorn
mangum
boto3
pydantic
pydantic-core
python-multipart
fastapi-clerk-auth
python-dotenv
openai
